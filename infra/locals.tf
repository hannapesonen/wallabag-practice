locals {
  # Standardized naming
  rg_name = "${var.project_name}-rg"
  acr_name = "${var.project_name}acr47382983772"
  pg_name = "${var.project_name}-pg"
  cae_name = "${var.project_name}-cae"
  app_name = "${var.project_name}-app"
}